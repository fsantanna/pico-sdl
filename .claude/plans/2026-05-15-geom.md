# 2026-05-15 — geom.hc cleanup + cv aspect-fill fix

## Status

- [x] rename `_vs_pos`/`_vs_rect` → `_root_pos`/`_root_rect`
- [x] move `_root_of` from CV section into VS section
- [x] simplify `_root_rect` r==NULL else-arm (drop `if (w==0||h==0)` branch)
- [x] add cv aspect-fill repro to `tst/cv.c`
- [ ] fix `L_name` typo at `_root_rect`:264 → `L->name`
- [ ] eager-resolve aspect-fill in `pico_set_scene_dst`
- [ ] remove dead `if (rect->w == 0)` in `_pico_output_draw_layer`
- [ ] collapse `_root_rect` else-arm to one-line cv wrapper

## Decisions

- Option **B** for `pico_set_scene_dim` post-dst:
  leave `dst` frozen, document the order (set dim before dst).
- No live-aspect tracking required by existing tests
  (audited tst/ and lua/tst/ on 2026-05-15).
- `_root_pos` is effectively a thin cv wrapper;
  `_root_rect` has real `r == NULL` semantics
  ("layer's visible rendered rect in root frame").

## Files touched so far

| file       | place                | change                                            |
|------------|----------------------|---------------------------------------------------|
| src/geom.hc | CV section header   | removed `_root_of` definition                     |
| src/geom.hc | VS section          | inserted `_root_of`                               |
| src/geom.hc | `_vs_pos` → `_root_pos`  | rename + callers                              |
| src/geom.hc | `_vs_rect` → `_root_rect` | rename + callers                             |
| src/geom.hc | `_root_rect` else-arm | dropped conditional aspect-fill resolve         |
| tst/cv.c   | end of main         | aspect-fill mid layer repro (currently failing)   |

## Bug: cv aspect-fill blind spot

`_cv_walk_*` resolves each layer's `scene.dst` via
`_sdl_rect(L->scene.dst, &Pb, NULL)` — third arg hard-coded `NULL`.
When `scene.dst.w == 0` or `.h == 0`, `_f_rat` returns the unfilled
zero, collapsing the projection scale to 0 and losing coordinate
info.

Repro: see `tst/cv.c` "aspect-fill mid layer" section.
Mid 50×50 under world 100×100 with `scene.dst = {'!', 0,0,0,100}`.
`(10,10) mid → world` should give `(20,20)` but cv returns `(0,20)`.

## Pending: eager normalization plan

When `pico_set_scene_dst` is called with `w==0` or `h==0`:
resolve immediately using current `scene.dim`'s aspect ratio
via `_f_rat`, store concrete `{w,h}`.

Ripple after eager:
- `_pico_output_draw_layer`:296-298 — dead, remove
- `_cv_walk_*` — works as-is (no zero w/h ever seen)
- `_root_rect` r==NULL else-arm — one-liner
  `pico_cv_rect(R->name, &ret, L->hier.up, &L->scene.dst)`
- `pico_set_scene_dim` — does NOT re-resolve dst (option B)
- API contract: callers must set `dim` before any aspect-fill `dst`

## Audit notes (existing tests)

| concern                                  | finding                                            |
|------------------------------------------|----------------------------------------------------|
| sets aspect-fill `dst` then changes `dim` | none                                              |
| uses aspect-fill `dst`                   | `tst/vs.c:256` only                                |
| sets `dst` then `dim`                    | none (`tst/keep.c` sets dim first, no zeros)       |
| Lua aspect-fill `dst`                    | none                                               |

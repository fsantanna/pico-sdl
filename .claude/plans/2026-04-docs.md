# Refactor lua api/guide

Branch: `2026-04-docs`.
Goal: review and refactor `lua/doc/api.md` and `lua/doc/guide.md`
to fix known gaps, improve consistency, and sync `lua/README.md`.

## 1. Scope

- `lua/doc/api.md` — reference doc
- `lua/doc/guide.md` — narrative walk-through
- `lua/README.md` — high-level sync
- Out of scope by default: C-side API changes
  (items #6, #8, #24 — see open questions)

## 2. Source TODOs (from `.claude/plans/TODO.md`)

| # | Item | Impact |
|---|------|--------|
| 2 | Use input `dt` in animation examples         | guide     |
| 3 | Introduce `pico.get.now` in guide            | guide     |
| 5 | Document realm hash / resource management    | guide+api |
| 7 | Review and complete guide (#103)             | guide     |
| 8 | `pico.set` vs multi-arg setters (#102)       | api       |
| 6 | Group draw state under `pico.set.draw.*`     | api (*)   |
| 24| Split `pico_set_view` into separate setters  | api (*)   |

`(*)` = requires API change; tracked as open question below.

## 3. Open questions

- [ ] Q1: Is #6 (`pico.set.draw.*` grouping) in scope?
      Or docs-only pass describing current API?
- [ ] Q2: Is #8/#24 (setter split) in scope for this branch?
- [ ] Q3: Any new sections to add to `guide.md`?
      Candidates: realm/resources, sub-layers, animation timing.
- [ ] Q4: Should `api.md` stay alphabetical,
      or regroup by concern (draw state, i/o, geometry, etc.)?

## 4. Steps

### 4.1. Audit

- [ ] Read `api.md` end-to-end; list inconsistencies.
- [ ] Read `guide.md` end-to-end; list missing topics / stale bits.
- [ ] Cross-check with `lua/tst/` examples to catch drift.
- [ ] Issue #103 — collect review comments.

### 4.2. Guide fixes (no API change)

- [ ] Introduce `pico.get.now` formally (item #3).
- [ ] Replace fixed increments with `dt`
      from `pico.input.event` in animation examples (item #2).
- [ ] Add realm/resource-management section (item #5).
- [ ] Address remaining review items from #103 (item #7).

### 4.3. API doc consistency pass

- [ ] Uniform signature style (args, return, mode prefixes).
- [ ] Ensure every operation has a one-line description.
- [ ] Verify Types section covers all `Rect`/`Pos`/`Dim` modes.
- [ ] Cross-links between api and guide.

### 4.4. README sync

- [ ] Update feature list / links if guide/api gain sections.

### 4.5. Regenerate assets (if needed)

- [ ] `lua/doc/gen-guide-*.lua` to refresh figures.
- [ ] Check `lua/doc/img/*.png` referenced sections still match.

## 5. Acceptance

- `api.md`:
    - every op has description, signature, optional notes
    - types section matches ops
- `guide.md`:
    - covers all items from #103 review
    - uses `dt` / `pico.get.now` where relevant
    - documents realm lifecycle
- `README.md` links are live.
- `make tests` in `lua/` still passes.

## 6. Progress

(update as work proceeds)

- [ ] 4.1 Audit
- [~] 4.2 Guide fixes
    - [x] §3.3 typo: missing `,` line 291
    - [x] §3.3 prose mismatch: replaced "Hello" red/bottom claim with
          two-text DejaVu example
    - [x] §3.3 image path: `'open.png'` → `'doc/img/open.png'`
- [ ] 4.3 API doc pass
- [ ] 4.4 README sync
- [~] 4.5 Assets
    - [x] `lua/tst/guide.lua` (§§2–3 only — guide.md ends at §3)
    - [x] guide.md: replaced all `*-XX.png` with concrete numbered names
          (`03-01-02`, `03-02-02..04`, `03-03-01..03`)
    - [ ] delete `lua/doc/gen-guide-images.lua` (deferred per user)
    - [ ] regenerate `img/guide-03-*.png` to match new tests

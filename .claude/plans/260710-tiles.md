# Plan: Tiles — Semantics, Consistency, Documentation

Sources analyzed:
    `src/geom.c`, `src/mem.c`, `src/get-set.c`, `src/layer.c`,
    `lua/pico.c`, `lua/doc/api.md`, `lua/doc/guide.md`.

## 1. Current Model

- `Tile = {w,h}`: tile size in the layer's **own texture pixels**
  (no mode tag).
- Stored per-layer in `scene.tile`; default `{0,0}` (= "no tiles").
- `'#'` mode resolves every value against the **current** layer's
  `scene.tile`:

```
pos:   raw = (x - 1 + anchor) * tile      -- 1-indexed, cell-anchored
dim:   raw = w * tile
rect:  raw = (x - 1 + anchor)*tile - anchor*(w*tile)
```

- Settable only via:

| Entry point       | Field  | Effect                                  |
|-------------------|--------|-----------------------------------------|
| `layer.empty`     | `tile` | dim × tile = texture size; stores tile  |
| `set.scene`       | `tile` | stores tile (applied **before** `dim`)  |

- Tile also drives the `_show_tile` grid aid (`layer.c:136`).

## 2. Ambiguities Found

### 2.1. `dim`/`tile` convention differs between entry points

| Entry point   | `dim={'!',5,5}, tile={20,20}`  | Result          |
|---------------|--------------------------------|-----------------|
| `layer.empty` | dim × tile **unconditionally** | 100x100 texture |
| `set.scene`   | `'!'` stays pixels             | 5x5 texture     |

- `layer.empty` (`mem.c:201`): multiplication happens for **any**
  dim mode when `tile` is present.
- `set.scene` (`get-set.c:330`): only `'#'` dim means tiles.
- Consequences in `layer.empty`:
    - `dim={'%',...}, tile=...`: percent of parent × tile
      (surely unintended).
    - `dim={'#',...}, tile=...`: resolves against the **current**
      layer's tile (`geom.c:392`), then × new tile again
      (**double multiplication**).

### 2.2. `'#'` without tile set: silent zero / division by zero

- `_raw_*` multiply by `tile=0`: silent zero-size results.
- `_rel_*` (`geom.c:525,561,601`) divide by `tile.w/h`:
  integer **division by zero** (UB/crash).
- No assert anywhere; api.md only says "(required for '#' dim)".

### 2.3. `'#'` ignores `base` (unlike `'%'`)

- `'%'` resolves against `base` (nested rect, parent, cv frame).
- `'#'` always reads `G.layer->scene.tile`, even when a `base` is
  supplied.
- Affects: `pico.cv.*`, `pico.xin.*`, `pico.vs.*`, `target` rects
  resolved in the **parent** frame, `layer.empty` dim with `up`.
- Which layer's tile *should* apply is undefined by design and
  undocumented.

### 2.4. `dim` vs `target` relation undocumented for tiles

- `dim` = texture resolution; `target` = placement in parent
  (`scene.dst`). Tile affects **only** `'#'` coordinate
  interpretation and the grid aid — never the dim→target mapping.
- `layer.empty` defaults `dim` from the `target` table
  (`lua/pico.c:1279`): with `target={'#',...}` or `'%'` the
  resulting texture size is unclear (and then × tile again).
- Nothing requires `dim % tile == 0`: fractional edge tiles are
  silently allowed.

### 2.5. `'#'` symbol overload — RESOLVED (260716, keep + document)

- `layer.images` `sheet={'#', w, h}` means "grid spec of the sprite
  sheet", unrelated to `scene.tile`. Same symbol, different meaning.
- Decision: **not really overloaded**. `'#'` uniformly means "count of
  grid cells along x/y"; §7 strengthens this (`dim`/`tile`/`pos`/`rect`
  all cell counts). Only the **grid source** differs:
    - `sheet` `'#'` → the sheet image's own `w×h` frame subdivision.
    - all others `'#'` → the layer's `scene.tile`.
- Action: **keep `'#'`**, no rename/API break. Doc-only (5.6):
  "`'#'` always counts grid cells; in `sheet` the grid is the sprite
  sheet's own `w×h` subdivision, elsewhere the layer's `scene.tile`."

### 2.6. Anchor/indexing semantics undocumented

- `'#'` is **1-indexed** (only stated in `pico.h:160` comment).
- In `'#'`, `x/y` name a **cell** and the anchor pins within the
  cell *and* within the shape; in `'!'`/`'%'`, `x/y` name a point.
- Not stated in api.md or guide.

## 3. What Is Consistent

- `_raw_/_rel_` handle `'#'` uniformly for pos/dim/rect.
- Lua `set.scene` applies `tile` before `dim` — `'#'` dim works.
- guide §4.1 example is correct for basic `'#'` drawing.
- `get/set.mouse`, `get.image`, `get.video`, `cv` document `'#'`.
- `Tile` type is listed in api.md Types.

## 4. What Is Missing (docs)

- [ ] 1-indexing and cell-anchor semantics of `'#'`.
- [ ] Whose pixels `tile` is in (layer-local texture px).
- [ ] `layer.empty`: dim×tile rule and its restriction to which
      dim modes (after 5.1 is decided).
- [ ] `'#'` requires a tile set; error behavior.
- [ ] Cross-layer `'#'` (cv/xin/vs/target): which tile applies.
- [ ] `set.scene.dim` forbids `'%'` (`assert`, `get-set.c:332`).
- [ ] `dim`/`target`/`tile` relation summary (one table in guide).

## 5. Steps

- [ ] 5.1 Adopt **mode-tagged `tile`** (`'!'`/`'#'`/`'%'`), resolved
      jointly with `dim` (see §7). `tile` becomes a `Rel_Dim`-like
      value; `dim`+`tile` resolve as a small dependency system with a
      single rule across `layer.empty` and `set.scene`.
    - promotes `Tile` type from plain `{w,h}` to tagged value.
    - touches `Tile` type, Lua parse (`lua/pico.c`), `geom.c`,
      `mem.c`, `get-set.c`, and all `tile` doc entries.
- [ ] 5.2 Fix `layer.empty` `'#'` double multiplication
      (`mem.c:185`): subsumed by §7 resolver — dim=`'#'` resolves
      against the **new** tile (tile=`'!'`), never the current layer's.
- [ ] 5.3 Guard `'#'` with unset tile (decided 260716, option A):
      `assert(tile.w>0 && tile.h>0)` (plain `assert`, matching
      `geom.c:397`) at the top of each `'#'` branch.
    - silent-zero on multiply (`_raw_*`): `geom.c:392,432,481,486`.
    - div-by-zero on divide (`_rel_*`): `geom.c:525,561`.
    - 6 branches total; rect reuses dim+pos.
    - complements §7 (creation-time `T>0`); 5.3 covers draw-time
      with no tile ever set.
- [ ] 5.4 **Doc-only** (downgraded 260716). Draw-time `'#'` is not a
      cross-layer problem: blit dst uses NULL `base` (`layer.c:263`),
      and every non-NULL `base` (`cv`/`xin`/`vs`/`in`) is a sub-rect
      of the **current** layer (resolved NULL-base first,
      `geom.c:214`), so `G.layer`'s tile is correct.
    - document: "`'#'` always uses the current layer's tile".
    - only residual: cross-layer coord conversion (`get.pos` S→T,
      `geom.c:160-206`) — one-line note on which tile it assumes.
    - no `base`-threading needed.
- [ ] 5.5 `dim % tile != 0`: **allow + document** (decided 260716,
      option A). No assert.
    - behavior: full cells exact; last row/col overhangs and clips
      to the texture edge (SDL render-target clip). No UB.
    - rationale: harmless (unlike 5.3 ÷0); legit borders/margins;
      erroring would contradict §7 — derived `tile=#`/`%` round
      (`⌊D/t⌋`) and almost never divide evenly.
    - doc note: "`dim` need not be a multiple of `tile`; the final
      row/column clips."
- [ ] 5.6 Update api.md: Types (`Tile`), `layer.empty`,
      `set.scene`, `'#'` mode entry (indexing, anchor, tile
      requirement).
- [ ] 5.7 Update guide §4.1: add `dim`/`target`/`tile` relation
      table, 1-indexing note, cross-layer note.

## 6. Progress

- 260710: plan created; source/docs analysis done (sections 1-4).
- 260716: designed mode-tagged `tile` (§7); revised 5.1/5.2;
  5.4 downgraded to doc-only (draw-time `'#'` is not cross-layer);
  5.3 decided (plain `assert` at 6 `'#'` branches);
  5.5 decided (allow frac tiles + document, no assert);
  §7 B decided (derived `T` floors, `⌊·⌋`);
  §7 D decided (omitted dim → target's dim spec, §7 unchanged);
  §2.5 decided (keep `'#'`, doc-only — grid source differs).
  All design decisions closed; remaining: docs (5.6/5.7) + impl.

## 7. Tile/Dim mode resolution (5.1 design)

`tile` gains a mode tag, resolved jointly with `dim`.
`P` = parent size (known).

### Definitions

Derived `T` floors to integer px (`⌊·⌋`); leftover clips (§5.5).

```
tile !   T = t          (pixels)
tile #   T = ⌊D / t⌋    (t = cells across dim)
tile %   T = ⌊t% · D⌋   (% of dim)

dim  !   D = d          (pixels)
dim  #   D = d · T      (d = cells)
dim  %   D = d% · P     (% of parent)
```

### All 9 combinations

| dim ↓ \ tile → | `!` (px)         | `#` (cells of dim) | `%` (of dim)       |
|----------------|------------------|--------------------|--------------------|
| `!` (px)       | `D=d`    `T=t`   | `D=d`    `T=D/t`   | `D=d`    `T=t%·D`  |
| `%` (of par)   | `D=d%·P` `T=t`   | `D=d%·P` `T=D/t`   | `D=d%·P` `T=t%·D`  |
| `#` (cells)    | `T=t`    `D=d·T` | ✗ circular         | ✗ circular         |

### Resolution order

```
dim == '#'  →  T first, then D = d·T   (needs tile absolute → tile='!')
else        →  D first, then T          (tile '!','#','%' all ok)
```

### Rejects

Both cycles are dim=`#` (needs T) × tile that needs D:

```
dim #, tile #  →  D=d·T, T=D/t   →  D = D·(d/t)     -- no anchor
dim #, tile %  →  D=d·T, T=t%·D  →  D = D·(d·t%)    -- no anchor
```

Rule: **dim=`#` is valid only with tile=`!`.**

### Omitted `dim` (§7 D, decided 260716)

`layer.empty` with no `dim` borrows the `target`'s dim spec
(`lua/pico.c:1288`; mode included). `target` is placed in the parent
(`up`) frame, which is already the `base` in `mem.c:196`, so
target=`'%'` = "% of parent". Only the target's **dim** is reused; its
**pos** still drives placement.

- decision: **no special case** — `dim := target.dim`, then the §7
  resolver applies **unchanged** (same matrix, rejects, floor).
- target=`'#'` needs tile=`'!'` (else reject, per the matrix).
- impl (5.1): tile parse at `lua/pico.c:1299` becomes a tagged
  `Pico_Rel_Dim` (read `mode`), threaded into the resolver.

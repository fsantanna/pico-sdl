# Plan: loop — 26-08-17

Fix `exit(0)` on quit event: unclean teardown + broken
`pico.input.loop()` escape.

# Problem

- `_event_handler` (`src/input.c:68`): quit event in
  aids + !expert → `exit(0)`
    - since `509e97c5` (2025-mar-01, v0.5)
    - rationale: novice scripts without quit handling
- Unclean teardown: skips `pico_init(0)`
    - same family as `260817-bug-segfault-exit.md`
- Breaks guide §6.3: ALT+F4 inside `pico.input.loop()`
  kills the process instead of escaping the loop

# Fix

- `pico_init(0)` before `exit(0)`
- `G.loop` flag set in `pico_input_loop`
    - `_event_handler` skips exit when set
    - quit propagates → `loop()` returns to prompt
- Note: `do_exit` param is vestigial (1 caller, always 1)

# Steps

- [x] `src/input.c`: `pico_init(0)` before `exit(0)`
- [x] `src/_pico.h` + `src/input.c`: `G.loop` flag
    - set/clear in `pico_input_loop`
    - skip exit in `_event_handler` when set
- [x] compile: `make lib` ok
- [ ] verify: ALT+F4 in `pico.input.loop()` returns to prompt
    - interactive, user
- [x] guide §6.3: wording correct again after fix (no edit)
- [x] `valgrind.supp`: `src/pico.c` unchanged (no update)

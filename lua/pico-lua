#!/bin/sh

VERSION=v0.4-dev

if [ "$1" = --version ]; then
    echo pico-lua $VERSION
    exit 0
fi

if [ $# -eq 0 ] || [ "$1" = --help ]; then
    echo "Usage: pico-lua [options] <file.lua>"
    echo ""
    echo "Options:"
    echo "  --help       Show this help"
    echo "  --version    Show version"
    exit 0
fi

PICO=$(dirname $(realpath "$0"))/../   # pico-sdl root

LUA=lua5.4
DIR=$(dirname -- "$1")  # /a/b/c
EXE=$(basename -- "$1") # x.lua

cd $DIR/
$LUA -lpico $EXE
